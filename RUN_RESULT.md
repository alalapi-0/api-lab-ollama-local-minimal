# RUN_RESULT

| 字段 | 值 |
| --- | --- |
| 真实本地模型运行 | 否 |
| 离线契约验证 | 通过（2026-08-12） |
| 验证模型占位名 | `test-local` |
| 真实推理耗时 | — |
| 未运行原因 | 当前环境没有 `ollama` 命令；服务与模型属于使用者控制的本地资源，因此没有安装、拉取模型或探测未知本地服务 |

## 验证范围

在一次性的 `git archive` 副本中，以内存桩替代 `requests` 和 `python-dotenv`，验证了：

- 缺少 `OLLAMA_MODEL` 时返回退出码 2，且不发起请求；
- 服务探测成功后，请求 `/api/chat` 的 URL、模型、消息、`stream`、`num_predict` 和超时参数符合契约；
- 成功响应会生成预期的 `output/result.json`；
- 服务连接失败与响应结构异常均返回退出码 1；
- `main.py` 通过 Python 语法编译检查。

验证结束后已移除一次性副本。上述响应内容与耗时均来自桩，不代表真实 Ollama 模型的质量或性能。
